name: Deploy Halo Docker Container

on:
  workflow_dispatch:  # 手动触发工作流

jobs:
  deploy:
    runs-on: ubuntu-22.04  # 使用已预装 Docker 的 Ubuntu 22.04

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Run Halo Docker container
      run: |
        docker run -d \
          --restart unless-stopped \
          --name halo \
          -p 8090:8090 \
          -e JAVA_OPTS="-Xms512m -Xmx1024m" \
          halohub/halo:latest

    - name: Install ngrok
      run: |
        curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc > /dev/null
        echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list > /dev/null
        sudo apt update
        sudo apt install -y ngrok

    - name: Login to ngrok
      run: |
        ngrok config add-authtoken 2qVHcdtl1O216lq18DfIKbEDf93_7RWJLoanfALo28D711aHB

    - name: Expose Halo and SSH with ngrok
      run: |
        ngrok http 8090 > /dev/null &
        ngrok tcp 22 > /dev/null &
        sleep 5
        echo "Halo public URL:"
        curl -s http://127.0.0.1:4040/api/tunnels | jq '.tunnels[] | select(.proto=="https") | .public_url'
        echo "SSH public URL:"
        curl -s http://127.0.0.1:4040/api/tunnels | jq '.tunnels[] | select(.proto=="tcp") | .public_url'

    - name: Keep Action Running
      run: sleep 3600  # 保持容器运行 1 小时
